import Link from 'next/link'
import { ArrowRight, Check, Clock3 } from 'lucide-react'
import { courses } from '@/lib/site-data'
import { Shell } from '@/components/site-shell'
export function generateStaticParams() { return courses.map(c => ({ slug: c.slug })) }
export default async function CoursePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const course = courses.find(c => c.slug === slug) ?? courses[0]; return <Shell><main><section className="bg-secondary/40"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-14 lg:grid-cols-[.85fr_1.15fr] lg:items-center lg:px-8 lg:py-24"><div><Link href="/courses" className="text-sm font-bold text-primary">← All courses</Link><p className="mt-12 text-xs font-bold uppercase tracking-[.22em] text-primary">{course.category} · {course.level}</p><h1 className="mt-4 font-serif text-5xl leading-tight sm:text-6xl">{course.title}</h1><p className="mt-6 max-w-lg text-lg leading-8 text-muted-foreground">{course.description}</p><div className="mt-8 flex flex-wrap gap-5 text-sm text-muted-foreground"><span className="flex items-center gap-2"><Clock3 className="h-4 w-4 text-primary" />{course.duration}</span><span className="font-semibold text-foreground">{course.price}</span></div><Link href={`/register?course=${course.slug}`} className="mt-9 inline-flex items-center gap-2 rounded-full bg-primary px-6 py-3.5 text-sm font-bold text-primary-foreground">Register for this course <ArrowRight className="h-4 w-4" /></Link></div><img src={course.image} alt="" className="aspect-[4/3] w-full rounded-[2rem] object-cover shadow-lg" /></div></section><section className="mx-auto grid max-w-7xl gap-12 px-5 py-16 lg:grid-cols-[1.1fr_.9fr] lg:px-8 lg:py-24"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-primary">What you will make</p><h2 className="mt-4 font-serif text-4xl">A course built around progress.</h2><p className="mt-6 max-w-xl leading-7 text-muted-foreground">Come with a question, leave with a project. Each session pairs a clear concept with time to practice, share, and get useful feedback.</p></div><div className="rounded-2xl border border-border p-6"><p className="font-semibold">In this course</p><ul className="mt-5 space-y-4">{course.lessons.map(lesson => <li key={lesson} className="flex gap-3 text-sm"><Check className="h-5 w-5 shrink-0 text-primary" />{lesson}</li>)}</ul></div></section></main></Shell> }
